$(document).ready(function(){
  // ---------------------------------------------------------
  // Activar menu responsive magnetico (home mi comunidad)
  // ---------------------------------------------------------
  $('#menu-magnetico').slicknav({
      prependTo:'#responsive-menu-magnetico'
  });
});
